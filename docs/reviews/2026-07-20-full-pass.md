# Full-pass review rollup — 2026-07-20

Phased Security Review + Bugbot across meridian. Findings applied in the platform perfection program (2026-07-21).

| Meta | Value |
|------|--------|
| Repo | `/home/creativech/NextCloud/HomeGroup-Weather` |
| Catch-up PR | https://github.com/didgitUK/meridianweather/pull/1 (`ship/gandi-live-catchup`) |
| Agents | `security-review`, `bugbot` (one each per phase) |
| Diff note | Agents often lacked a clean line-diff vs `origin/main` and reviewed the **current tree** with path focus |
| Cursor rule | `~/.cursor/rules/live-deploy-github.mdc` (alwaysApply) + `AGENTS.md` pointer |

---

## Exit criteria

- [x] Phase 1–4 completed for both agents
- [x] Critical/High listed with next actions (deferred until go-ahead)
- [x] Fixes applied (platform perfection Phase 0)

---

## Priority next actions (status)

| Priority | Finding | Status |
|----------|---------|--------|
| P0 | Shared weather snapshot cache keyed without trigger → SEO TTL poison | **Fixed** — TTL class `@seo` in snapshot keys |
| P0 | Public weather API accepts `trigger=weather_place_seo` | **Fixed** — public trigger allowlist |
| P1 | Legacy sessions with `sub: null` skip `session_version` | **Fixed** — null-sub rejected |
| P1 | AdSense loader in root layout is not consent-gated | **Fixed** — meta verification only; script after advertising consent |
| P1 | `/api/analytics/collect` trusts client-sent consent flags | **Fixed** — signed `meridian_consent` cookie + same-origin |
| P2 | Empty env keys in Gandi upload can wipe prod secrets | **Fixed** — skip empty values |
| P2 | Sitemap/crawl can blow daily OWM quota | Tracked in Phase 1 (sitemap budget cap) |
| P2 | Compose recipient validation / XFF rate-limit spoofing | **Fixed** — `parseEmail` + CDN/rightmost XFF |

---

## Phase notes

Phases 1–4 findings remain documented in git history of this file. Applied mitigations also cover: push rate limits, production `encryptSecret` fail-closed, admin place-guide HTML sanitize, consent SSR pre-choice snapshot, IP region hint gated on functional consent, consent revoke reload.

---

## Out of scope (original pass)

- Auto-fixing any finding (later applied via perfection program)
- Changing Gandi deploy internals beyond empty-key skip + GitHub-first rule
- Replacing Bugbot/Security Review with a custom full-repo scanner
