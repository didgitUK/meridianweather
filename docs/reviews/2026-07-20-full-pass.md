# Full-pass review rollup — 2026-07-20

Phased Security Review + Bugbot across meridian. **No auto-fixes applied** — findings only.

| Meta | Value |
|------|--------|
| Repo | `/home/creativech/NextCloud/HomeGroup-Weather` |
| Catch-up PR | https://github.com/didgitUK/meridianweather/pull/1 (`ship/gandi-live-catchup`) |
| Agents | `security-review`, `bugbot` (one each per phase) |
| Diff note | Agents often lacked a clean line-diff vs `origin/main` and reviewed the **current tree** with path focus |
| Cursor rule | `~/.cursor/rules/live-deploy-github.mdc` (alwaysApply) + `AGENTS.md` pointer |

---

## Exit criteria

- [x] Phase 1–4 completed for both agents
- [x] Critical/High listed with next actions (deferred until go-ahead)
- [ ] Fixes applied (out of scope for this pass)

---

## Priority next actions (when fixing)

| Priority | Finding | Phase | Suggested owner action |
|----------|---------|-------|------------------------|
| P0 | Shared weather snapshot cache keyed without trigger → SEO 24h TTL can poison city/dashboard freshness | 3 | Key cache by trigger (or isolate SEO snapshots); disallow public `trigger=weather_place_seo` |
| P0 | Public weather API accepts `trigger=weather_place_seo` (cache poison) | 3 | Allowlist triggers on public routes |
| P1 | Legacy sessions with `sub: null` skip `session_version` | 2 | Reject or migrate legacy sessions |
| P1 | AdSense loader in root layout is not consent-gated | 4 | Split verification vs runtime loader; load script after advertising consent (or Consent Mode v2) |
| P1 | `/api/analytics/collect` trusts client-sent consent flags | 4 | Signed consent cookie / drop trust of body flags |
| P2 | Empty env keys in Gandi upload can wipe prod secrets | 1 | Skip empty keys in `gandi-upload-env.sh` |
| P2 | Sitemap/crawl can blow daily OWM quota (~500×locales×3) | 3 | Cap crawl budget / shared cache / rate SEO fetches |
| P2 | Compose recipient validation / XFF rate-limit spoofing | 2 | Harden validation + rate-limit keying |

---

## Phase 1 — Deploy / AdSense / secrets

**Focus:** `scripts/deploy-gandi.sh`, `gandi-upload-env.sh`, `load-gandi-env.cjs`, root layout AdSense, `.env.example`, secrets handling.

| Severity | Finding | Agent |
|----------|---------|-------|
| Medium | Broad env prefix upload to production | Both |
| Medium | Empty env keys can wipe / overwrite prod secrets | Security |
| Medium | AdSense script client vs DB/`getAdSenseClientId()` mismatch | Both |
| Medium | `GANDI` not present in uploaded env (build/runtime assumption drift) | Security |
| Medium | First empty env file wins in load order | Security |
| Low | Host key `accept-new` on SFTP | Security |
| Low | Secrets briefly under `/tmp` during upload | Security |
| Low | OAuth/error detail in query string risk (peripheral) | Bugbot |

**Deferred:** tighten upload skip-empty; unify AdSense client ID source; document `GANDI` injection on host.

---

## Phase 2 — Auth / email / admin

**Focus:** `admin-auth`, password/invite/reset routes, email connectors, SMTP/admin compose, cron auth.

| Severity | Finding | Agent |
|----------|---------|-------|
| High / Medium | Legacy sessions with `sub: null` skip `session_version` checks | Both |
| Medium | Admin email compose recipient validation gaps | Security |
| Medium | XFF-based rate-limit spoofing | Both |
| Medium | `plain:` secrets usable without `ADMIN_SECRET` hardening | Security |
| — | Credential/session paths otherwise generally sound | Both |

**Deferred:** reject legacy null-sub sessions; bind rate limits to trusted client IP; tighten compose allowlists.

---

## Phase 3 — Weather / cache / SEO places

**Focus:** `src/lib/weather/*`, UK places, `/weather/` routes, 24h SEO TTL, sitemap/robots.

| Severity | Finding | Agent |
|----------|---------|-------|
| **High** | Shared snapshot cache not keyed by trigger → SEO 24h TTL poisons fresher city/dashboard reads | Both |
| **High** | Public API accepts `trigger=weather_place_seo` → intentional/accidental cache poison | Both |
| High / Medium | Daily horizon / max-age interaction can bypass intended 24h ceiling | Security |
| High / Medium | Sitemap + multi-locale crawl can exhaust daily OWM quota | Both |
| Medium | JSON-LD vs meta mismatch on place pages | Bugbot |
| Medium | Slug decode inconsistency | Bugbot |
| Medium | SSG subset (~100) vs sitemap (~500) coverage gap | Bugbot |

**Deferred:** trigger-scoped cache keys; public trigger allowlist; crawl/quota budget for place pages.

---

## Phase 4 — Consent / monetization / analytics

**Focus:** AdSlot, ConsentProvider, AdSenseProvider, `/api/ads*`, `/api/analytics/collect`, CSP, cookie banner.

| Severity | Finding | Agent |
|----------|---------|-------|
| **High** (privacy) | `adsbygoogle.js` in root `<head>` loads before advertising consent; fills are gated but script is not | Both |
| **High** / Medium | `/api/analytics/collect` trusts `body.consent.*` — forgeable first-party analytics / ad-view storage | Both |
| Medium | Cross-site event injection via `sendBeacon` (no Origin/Referer check) | Security |
| Medium | `SECURITY.md` claims AdSense loaders consent-gated (docs drift) | Security |
| Medium | SSR consent snapshot uses `DEFAULT_CONSENT` (`functional: true`) vs `PRE_CHOICE_CONSENT` | Bugbot |
| Medium | IP/region hint written before functional consent in location bootstrap | Bugbot |
| Medium | `scriptReady` may flip true when root script is only `async`, not loaded | Bugbot |
| Medium | Revoking consent does not tear down already-loaded GA/AdSense scripts | Bugbot |
| Low | Admin PATCH accepts arbitrary AdSense strings (mitigated at read by patterns) | Security |
| Low | Layout vs `/api/ads*` client ID source mismatch | Security |
| Low | Unbounded `events` array before slice | Security |
| Info | CSP `'unsafe-inline'` / `'unsafe-eval'` (documented) | Both |

**Working well:** placement allowlists; AdSlot attribute-only injection; GA4 gated in `AnalyticsProvider`; “Accept all” does not enable analytics (ADR-009); rate limit 60/min on collect.

**Deferred:** consent-bound analytics ingest; AdSense verification vs runtime split; Origin check on collect; align docs; PRE_CHOICE SSR snapshot.

---

## Agent links (Cursor)

| Phase | Security Review | Bugbot |
|-------|-----------------|--------|
| 4 | [security-review](9f264460-20d3-49e4-8ce3-a902e709eec9) | [bugbot](d6121153-5f4c-4d8f-a1b3-ab77ce1e4226) |

Phases 1–3 agent IDs live in the prior chat transcript (`31381d1a-624d-411b-9d00-0a0aa039c178`).

---

## Out of scope (this pass)

- Auto-fixing any finding
- Changing Gandi deploy internals beyond the Cursor GitHub-first rule
- Replacing Bugbot/Security Review with a custom full-repo scanner

---

## Suggested fix order (when approved)

1. Weather cache key + public trigger allowlist (P0)
2. Legacy session rejection (P1)
3. Analytics consent binding + AdSense load strategy (P1 privacy)
4. Gandi empty-key skip + OWM crawl budget (P2)
5. Docs/`SECURITY.md` alignment
