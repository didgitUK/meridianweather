# Public client env baked at `next build` time.
# GA Measurement IDs are not secrets (they appear in page source when GA loads).
# Required on Gandi because the build VM may not see /srv/data/home/meridian.env.
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-QWS3EPNZCL
